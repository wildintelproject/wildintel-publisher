## About this record

{% if self_contained %}
This is a **self-contained** B2SHARE record: `{{ zip_filename }}` bundles the whole
repository (minus `.git/`), at the version cited below — everything needed to use the
software, in a single archive. The loose files are not published separately; only
`{{ zip_filename }}` (plus this README, license and citation) makes up the record.

```text
.
├── {{ zip_filename }}          ← self-contained package: the software's source code
├── checksums-sha256.txt
├── CITATION.cff
└── LICENSE
```

## B2SHARE (EUDAT) record

This record, including `{{ zip_filename }}`, is hosted on [B2SHARE](https://b2share.eudat.eu/),
EUDAT's European research-data repository.
{% else %}
This is a **reference-only** B2SHARE record — it does not host the source code itself,
only its citation metadata (this README and `CITATION.cff`). This record exists to give
the software a European-hosted (EUDAT), permanent, citable PID/DOI, independent of
wherever its source code repository lives.

## B2SHARE (EUDAT) record

This record is hosted on [B2SHARE](https://b2share.eudat.eu/), EUDAT's European
research-data repository.
{% endif %}
{% if homepage %}

The software's source code is hosted at:

```text
{{ homepage }}
```
{% endif %}
